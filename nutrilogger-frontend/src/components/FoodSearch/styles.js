import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    foodSearchForm: {
        display: 'flex',
        flexWrap: 'wrap',
        justifyContent: 'center'
    },

    paper: {
        padding: '20px'
    }
}));